<% provide(:title, '404') %>
<% provide(:layout_override, 'devise') %>

<%= render partial: 'templates/errors/partials/error', locals: {
  signed_in: true,
  prev_page: true,
  code: 404,
  title: 'Page Not Found',
  signed_in_message: "Sorry but it looks like the page you were looking for doesn't exist. You may have mistyped the address or the page may have moved.",
  signed_out_message: "Sorry but it looks like the page you were looking for doesn't exist. You may have mistyped the address or the page may have moved, or you may need to login to access the page.",
} %>
